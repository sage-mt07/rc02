## 2025-09-12 23:30 JST [詩音]
manual commit の物理テスト準備と examples 棚卸しを実施。

- 物理テスト（manual commit）
  - 制約: `Kafka.Ksql.Linq.Tests.Integration` は実行禁止のため、テストフレームワークは不使用
  - 対応: 簡易ランナーを作成し、同等シナリオを再現
    - 追加: `physicalTests/Runners/ManualCommitRunner/`（csproj + Program.cs）
    - 構成: ローカル docker-compose で Kafka/SchemaRegistry/ksqlDB を起動（既存コンテナ競合回避のためポートを 39092/18081/18088 に退避）
    - 期待: 1..5 を Produce、3 で Commit、再開時は 4 から消費
  - 実施状況: 環境起動は OK / ランナー実行はブローカー advertised listeners の二重定義により、ホスト側クライアントが `kafka:29092` 宛を参照して接続失敗
    - 回避案A: ランナーを同一 docker ネットワーク内（コンテナ）で実行
    - 回避案B: Kafka をホスト公開のみの advertised listeners にし、内部コンシューマ（Schema Registry 等）もホスト名/ポートで接続させる構成に寄せる
  - 次アクション（提案）: A案で検証を継続（.NET SDK コンテナでランナー実行）

- examples 棚卸し（動作しない一覧作成）
  - 結果: `docs/diff_log/diff_examples_status_20250912.md` に記録
  - 主因: 属性名の刷新（KsqlTopic/KsqlDecimal/KsqlTimestamp 等）未追従、`KsqlContext` 生成 API 変更、`manual-commit` のポート不整合/ csproj 不在、`headers-meta` のリテラル崩れ

