## 2025-09-04 09:40 JST [tenjo]
物理テスト（per-test compose up/down）を実行し、失敗群の一次切り分けと即応修正を実施。

- 実行: `tools/run_physical_tests_per_test.sh`
- 出力: `Reportsx/physical/20250903-235641/`
- 結果: Passed 20 / Failed 15 / Total 37

対応1（Naruse/T1 完了）
- 不具合: `KafkaAdminServiceIntegrationTests.EnsureTopic_CreatesWithConfiguredStructure` が `ArgumentException: metadata.max.age.ms=0` で失敗。
- 原因: `KafkaAdminService` が `KsqlDslOptions` の DefaultValue を適用せず、`MetadataMaxAgeMs=0` が Confluent クライアントへ渡っていた。
- 対応: `KafkaAdminService` で `DefaultValueBinder.ApplyDefaults(_options)` を適用し、`MetadataMaxAgeMs<=0` の場合は未設定（ライブラリ既定）とする安全化。
  - 変更: `src/Infrastructure/Admin/KafkaAdminService.cs`
  - 検証: 単体再実行で Pass（`Reportsx/physical/20250904-003840/`）。

対応2（Naruse/T2 進行）
- 不具合: `KafkaServiceDownTests.*` が `ArgumentNullException`（type=null）で失敗。
- 原因: キー無しエンティティの送信で `AvroKeyType==null` を前提化しておらず、`Activator.CreateInstance(null)` 発生。
- 対応: キー無しは `Producer<Null,TValue>` を使用し、シリアライズは値側のみ。キー生成・設定を分岐。
  - 変更: `src/Messaging/Producers/KafkaProducerManager.cs`
  - 現状: 例外は `ProduceException(Local: Value serialization error)`（Schema Registry 到達時）に遷移。期待条件（Kafkaダウン時の適切な例外インジケータ）に合致するか再評価が必要（詩音・鏡花へ依頼）。

未着手・調査継続（抄）
- `BigBang_KafkaConnection_TolerantTests.*`（許容系）
- `DefaultAndBoundaryValueTests.*`（精度/既定値）
- `JoinIntegrationTests.TwoTableJoin_Query_ShouldBeValid`
- `ManualCommitIntegrationTests.ManualCommit_PersistsOffset`
- `KsqlSyntaxTests.*` の一部（生成KSQLの妥当性）

優先T1〜T5（Shion/Jinto/Kyouka 配分）
1. Kafkaダウン系の期待例外整合（ArgNull→Kafka系/serialization系）とメッセージ基準（Shion/Kyouka）
2. Default値の適用漏れ総点検（Admin/Producer/Consumer 各Config）と最小再現のユニット化（Jinto）
3. Join/Topic構成: `EnsureTopic` と JOINテストの失敗原因の切り分け（Naruse）
4. Decimal/Boundary: グローバル精度・スケールの適用検証と不足時の既定値定義（Naruse/Jinto）
5. KSQL生成の失敗事例収集（invalid/validの境界）→最小ケースのDSL→KSQL期待比較のユニット追加（Shion/Jinto）

メモ
- 物理系テストは per-test 起動で `Reportsx/physical/<UTC>/NNN_*` に `dotnet_test.log`/composeログを保存済み。再現は `tools/run_physical_tests_per_test.sh` の `--filter` 相当で可能。

