# Expression analysis metadata windows inputs

- `ExpressionAnalysisResult.ToMetadata` now registers `input/{tf}Live` and `input/{tf}Final` for every timeframe in `Windows`.
- Tests updated to rely on `ToMetadata` for input keys.
