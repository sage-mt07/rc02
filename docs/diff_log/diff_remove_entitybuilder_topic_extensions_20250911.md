# EntityBuilder topic extensions removal

- Removed obsolete `EntityBuilderTopicExtensions` exposing topic configuration API via `IEntityBuilder<T>`.
- Use `EntityModelBuilder` methods directly for topic settings.
- Documented direct usage in `docs/api_reference.md`.
