# diff: docs cleanup (remove dev_guide) 2025-09-05

## Summary
- Removed `docs/dev_guide.md` per user request: user-facing docs must only contain actionable usage; the prior file mixed internal guidance and duplicated content.
- Updated `README.md` to remove links to the deleted page and to `getting-started.md` entries that were link-only.

## Impact
- Broken links eliminated in `README.md`.
- Examples remain the primary runnable references (e.g., `examples/basic-produce-consume`).

## Follow-ups (optional)
- Replace any remaining link-only pages with content or remove them.
- Ensure example READMEs point directly to runnable steps (not intermediary docs).

